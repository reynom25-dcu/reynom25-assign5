<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marcusreynolds.sailtales" >

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ListTrips"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:theme="@android:style/Theme.DeviceDefault.Light.Dialog"
            android:name=".UpdateTrip"
            android:label="@string/app_name" />

        <activity android:theme="@android:style/Theme.DeviceDefault.Light.Dialog"
            android:name=".AddTrip"
            android:label="@string/add_trip" />

        <activity android:theme="@android:style/Theme.DeviceDefault.Light.Dialog"
            android:name=".Summary"
            android:label="Trip Summary" />

        <activity android:theme="@android:style/Theme.DeviceDefault.Light.Dialog"
            android:name=".RSSReader.SimpleRSSReaderActivity"
            android:label="Sailing News" />
    </application>

</manifest>
